import Loader from "./components/Loader";

// Loading component to display a loading indicator
const Loading = () => {
  // Render the Loader component to indicate that content is loading
  return (
    <Loader />
  );
}

export default Loading;
